import { AvatarName } from "./Avatar.name";
import { AvatarProfilePhoto } from "./Avatar.ProfilePhoto";
import { AvatarRoot } from "./Avatar.root";

export const Avatar = {
    root: AvatarRoot,
    profilePhoto: AvatarProfilePhoto,
    name: AvatarName
}